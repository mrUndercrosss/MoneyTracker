class Income:

    def __init__(self, name, category, amount, date, user_id) -> None:
        self.name = name
        self.category = category
        self.amount = amount
        self.user_id = user_id
        self.date = date
